import * as componentComponent from './commercial-details.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './commercial-details.component.scss';

// Add style imports above

const componentModule = angular.module('commercialDetails', [
// Add module dependencies above
]);

componentModule.component('commercialDetails', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

import * as componentComponent from './testimonials.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './testimonials.component.scss';

// Add style imports above

const componentModule = angular.module('testimonials', [
// Add module dependencies above
]);

componentModule.component('testimonials', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

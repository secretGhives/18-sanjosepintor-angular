import * as componentComponent from './residential-details.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './residential-details.component.scss';

// Add style imports above

const componentModule = angular.module('residentialDetails', [
// Add module dependencies above
]);

componentModule.component('residentialDetails', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

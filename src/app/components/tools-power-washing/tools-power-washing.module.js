import * as componentComponent from './tools-power-washing.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './tools-power-washing.component.scss';

// Add style imports above

const componentModule = angular.module('toolsPowerWashing', [
// Add module dependencies above
]);

componentModule.component('toolsPowerWashing', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

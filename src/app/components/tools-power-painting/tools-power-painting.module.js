import * as componentComponent from './tools-power-painting.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './tools-power-painting.component.scss';

// Add style imports above

const componentModule = angular.module('toolsPowerPainting', [
// Add module dependencies above
]);

componentModule.component('toolsPowerPainting', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

import * as componentComponent from './tools-safety-floor.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './tools-safety-floor.component.scss';

// Add style imports above

const componentModule = angular.module('toolsSafetyFloor', [
// Add module dependencies above
]);

componentModule.component('toolsSafetyFloor', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

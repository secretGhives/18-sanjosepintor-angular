import * as componentComponent from './tools-safety-personal.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './tools-safety-personal.component.scss';

// Add style imports above

const componentModule = angular.module('toolsSafetyPersonal', [
// Add module dependencies above
]);

componentModule.component('toolsSafetyPersonal', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

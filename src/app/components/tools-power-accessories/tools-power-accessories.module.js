import * as componentComponent from './tools-power-accessories.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './tools-power-accessories.component.scss';

// Add style imports above

const componentModule = angular.module('toolsPowerAccessories', [
// Add module dependencies above
]);

componentModule.component('toolsPowerAccessories', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;

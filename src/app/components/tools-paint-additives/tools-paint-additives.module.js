import * as componentComponent from './tools-paint-additives.component';

// Add module imports above

// Add directive imports above

// Add service imports above

import './tools-paint-additives.component.scss';

// Add style imports above

const componentModule = angular.module('toolsPaintAdditives', [
// Add module dependencies above
]);

componentModule.component('toolsPaintAdditives', componentComponent);

// Add directive to module above

// Add service to module above

export default componentModule;
